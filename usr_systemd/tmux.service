[Unit]
Description=Persistent Tmux Server

[Service]
Type=forking
ExecStart=/usr/bin/tmux new-session -s default -d
ExecStop=/usr/bin/tmux kill-session -t default
# Protect from systemd-oomd
OOMPolicy=continue
ManagedOOMPreference=avoid

[Install]
WantedBy=default.target
